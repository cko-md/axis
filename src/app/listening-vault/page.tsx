import { AppShell } from "@/components/layout/AppShell";
import { VaultModule } from "@/components/vault/VaultModule";

export default function ListeningVaultPage() {
  return (
    <AppShell section="Life" page="Listening Vault">
      <VaultModule />
    </AppShell>
  );
}
