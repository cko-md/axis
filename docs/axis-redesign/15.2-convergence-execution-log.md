# Wave 15.2 Convergence — Execution Log

Branch: `codex/vector-phase15-convergence` (coordinator-owned; not pushed, not merged).
Base: `origin/codex/phase9-resume-executor` @ `84cc5ca6` (PR #195).
Ported: VECTOR arcade platform from `origin/codex/vector-arcade-envoys` @ `a72f3da7` (PR #236).

## Verified remote heads (post-fetch)
| ref | head |
|---|---|
| origin/main | `a029cd58` |
| origin/codex/phase9-resume-executor (#195) | `84cc5ca6` |
| origin/codex/vector-arcade-envoys (#236) | `a72f3da7` (brief's `4e44e245` is +1 docs commit behind) |
| origin/codex/electron-preview-release (#227) | `180fad35` |

## Branch-graph decision
VECTOR's substantive work is nearly one commit: `4e44e245` ("ship shared arcade platform", 91 files,
+24.9k). The lifecycle migration `202607161000_lifecycle_claims.sql` was introduced by the *merge*
`0753eada`, NOT by the platform commit. Convergence therefore **cherry-picks only `4e44e245`** onto
phase9 — automatically excluding VECTOR's lifecycle migration and its service code.

## Slice 1 — schema + platform port  → commit `4d4c245c`
- Cherry-picked `4e44e245` (`-x --no-commit`); resolved 14 conflicts:
  - 6 code files (getAppOrigin, middleware, AppShell, Sidebar, Modal, database.types) — see commit body.
  - 4 VECTOR-only docs (`.logs/vector-envoys/INDEX.md`, `15-completion-matrix.md`,
    `15-vector-arcade-and-envoys.md`, `docs/vector/PLAN.md`) → took theirs.
  - 4 state/verification docs (PROGRAM_STATE, DEFECT_LEDGER, 13-final-verification, production-readiness)
    → kept phase9 canonical; rewritten in slice 5.
- **Migration identity resolved:** renamed `202607161200_vector_arcade_persistence.sql`
  → `202607170001_vector_arcade_persistence.sql` (collided with phase9's
  `202607161200_entity_workspace.sql`). Updated the two doc references. VECTOR SQL tests reference table/
  function names, not the version, so they were unaffected.
- **database.types.ts:** took phase9's complete types + spliced ONLY the 6 `game_*` tables and 3 game RPCs
  (`sync_vector_save`, `apply_vector_event`, `resolve_vector_conflict`). No VECTOR lifecycle RPC types added.

## Slice 2 — lifecycle RPC rewire  → NO-OP (verified)
The platform commit touched zero core lifecycle files. The convergence tree calls only phase9 canonical
RPCs (`cas_agent_task_transition`, `consume_actionable_approval`, `claim_routine_resume`) and **no** VECTOR
lifecycle RPCs (`create_agent_task`/`transition_agent_task`/`execute_approval`/`*_routine_approval_resume`
are absent from the tree). The invariant-matrix rewire was therefore unnecessary — the platform was cleanly
separable from VECTOR's lifecycle. Confirms the decision to drop `202607161000_lifecycle_claims.sql`.

## Final migration inventory (convergence)
```
202607161200_entity_workspace.sql          (phase9, hosted-track)
202607161300_task_approval_atomic.sql      (phase9, hosted expand)
202607161302_webauthn_atomic.sql           (phase9, hosted expand)
202607161400_routine_resume_claims.sql     (phase9, hosted expand)
202607161401_task_approval_lockdown.sql    (phase9, contract — BLOCKED until prod SHA live)
202607170001_vector_arcade_persistence.sql (VECTOR, renumbered — additive; RLS/grant review pending)
```

## Gates that CANNOT be executed in this environment (honest)
- No Supabase CLI / local stack → arcade migration `202607170001` RLS/grant review is **manual, not replayed**.
- Fresh-Supabase authenticated Playwright cannot run here → browser gate stays **NOT GREEN**.
- Hosted Supabase/Vercel/Sentry/Render/OpenAI remain **BLOCKED** (owner).

## Local gates executed
See the verification section of the wave log / PROGRAM_STATE after slice 5.
