# Wave 15.2 Convergence — Migration Reconciliation & Convergence Plan (analysis only)

Base pinned to fetched remote heads. No writes to shared branches; nothing renamed/applied/merged.

## Verified heads
| ref | head |
|---|---|
| origin/main | `a029cd58` |
| origin/codex/phase9-resume-executor (#195, canonical base) | `84cc5ca6` |
| origin/codex/vector-arcade-envoys (#236) | `a72f3da7` (brief's `4e44e245` is stale; a72f3da7 = +1 docs commit) |
| origin/codex/electron-preview-release (#227) | `180fad35` |

## Combined migration inventory (2026-07-16 window)
| version | phase9 (#195) | VECTOR (#236) | disposition |
|---|---|---|---|
| 202607161000 | — | `lifecycle_claims.sql` | **DROP** (superseded; see invariant matrix) |
| 202607161200 | `entity_workspace.sql` (entity_references, entity_usage) | `vector_arcade_persistence.sql` (game_* tables) | **COLLISION** → phase9 keeps version; VECTOR renumbers |
| 202607161300 | `task_approval_atomic.sql` | — | keep (hosted expand) |
| 202607161302 | `webauthn_atomic.sql` | — | keep (hosted expand) |
| 202607161400 | `routine_resume_claims.sql` | — | keep (hosted expand) |
| 202607161401 | `task_approval_lockdown.sql` | — | keep, **BLOCKED** (contract; apply only after compatible prod SHA live) |

## Decision A — version collision `202607161200`
- `202607161200_entity_workspace.sql` (phase9): **unchanged.** It is on the canonical base and on the hosted track.
- `202607161200_vector_arcade_persistence.sql` (VECTOR): **local-only, not hosted → renumber.**
  - **Proposed target: `202607170001_vector_arcade_persistence.sql`** (next unused after max `202607161401`; today = 2026-07-17).
  - Arcade tables (`game_profiles`, `game_events`, `game_saves`, `game_scores`, `game_achievements`,
    `game_save_conflicts`) and functions (`sync_vector_save`, `apply_vector_event`, `resolve_vector_conflict`)
    are **additive and independent** of the lifecycle chain, so sorting *after* `1401` is correct and safe.
  - **Rename fan-out (all must change atomically in the port slice):**
    filename · VECTOR SQL test harnesses asserting the version · `docs/vector/*` · `15-completion-matrix.md` ·
    `PROGRAM_STATE.json` · release manifest/checksums · `VE-CONTINUE-CLAUDE.md` + prompts · migration-history
    snapshot/assertion tests.
  - **Do not** touch `1300/1302/1400` (hosted). **Do not** apply `1401` early.

## Decision B — lifecycle authority
Drop VECTOR `202607161000_lifecycle_claims.sql`; adopt phase9 `1300/1302/1400/1401`. Rewire VECTOR's ~6 RPC
call-sites (see invariant matrix). Reason: phase9 is a strict conceptual superset (WebAuthn/passkey, idempotent
creation, scope-completeness, stale-approval expiry, browser-DML lockdown) and follows the mandatory
expand→application→contract order; VECTOR's monolith (`202607161000`) sorts *before* the phase9 chain and would
install a second contradictory authority.

## Canonical release order (unchanged, reaffirmed)
```
expand:      202607161200_entity_workspace
             202607161300_task_approval_atomic      (hosted)
             202607161302_webauthn_atomic           (hosted)
             202607161400_routine_resume_claims     (hosted)
  -> compatible application live
contract:    202607161401_task_approval_lockdown    (BLOCKED — needs prod SHA + recovery owner + rollback)
additive:    202607170001_vector_arcade_persistence (renumbered; RLS/grant review + generated types + local replay)
```

## Proposed convergence sequence (each = one reviewed slice; owner-gated items flagged)
1. **[this deliverable]** Reconciliation analysis + invariant matrix + PR_RECONCILIATION.json — review before any write.
2. Create `codex/vector-phase15-convergence` from `origin/codex/phase9-resume-executor@84cc5ca6` (worktree; coordinator-owned).
3. Port slice 1 — **schema**: add `202607170001_vector_arcade_persistence.sql` (renumbered); drop VECTOR lifecycle
   migration; regenerate Supabase types; local SQL replay + RLS/grant review; VECTOR concurrent-CAS suite (arcade only).
4. Port slice 2 — **lifecycle rewire**: repoint VECTOR src RPC call-sites to phase9 names; run the AUDIT rows (2,10,12) first.
5. Port slice 3 — **VECTOR platform**: routes/registry/loaders/runtime host/IndexedDB/CAS/offline SW/modal a11y/telemetry.
6. Repair checkpoint docs (`PROGRAM_STATE.json`, `VE-CONTINUE-CLAUDE.md`) with real Wave 15.2 numbers.
7. Restore the stronger CI (fresh-Supabase authenticated Playwright + lifecycle SQL contract + VECTOR CAS suites).
8. Independent read-only review (non-overlapping file sets), then Wave 15.3 Second Sense.

## Still BLOCKED (owner) — do not convert to a pass
Hosted Supabase DDL/readback · Vercel authorized preview validation · Sentry issues/dashboards · Render worker ·
OpenAI paid generation · signed release · merge/deploy authorization · contract migration `202607161401` timing.

## PROGRAM_STATE.json staleness (confirmed on VECTOR branch — repair in step 6)
- `revision_reviewed: a029cd58` → should be convergence head.
- `unit_tests: 919 / 134 files` → Wave 15.2 result is `1104 / 152 files`.
- `next_dependency_ordered_actions` still says "commit and push Wave 15.2" (already pushed) → replace with convergence.
