# Migration Reconciliation Audit — phase9 (#195) vs prod-corrected main vs live prod

> **SCOPE CORRECTION (prod-authoritative, added after live `schema_migrations` review).**
> The initial phase9-vs-main file diff **undercounted** the drift, because *both* repos carry the same
> 12-digit placeholder versions — so comparing them to each other hid the divergence from prod. Checking
> against live prod's `schema_migrations` (82 migrations, 14-digit timestamps from `20260610`) shows the real
> scope: **~15 July migrations are version-mismatched** (not 7), there is a **double-applied** `050_workout_logs`,
> and **2 phase9 migrations are absent from prod**. The full prod-authoritative map is in
> "Complete drift map" below; the earlier Category A/B content verification remains valid (those objects are
> prod-faithful) but the RENAME set is larger. **Do not execute a partial 7-rename — it would misrepresent the
> fix.** This is a dedicated migration-history reconciliation project (owner re-scope required).

## Complete drift map (prod `schema_migrations` = ground truth)
| phase9 file | prod version | prod name | disposition |
|---|---|---|---|
| 202607021200_calendar_event_cache | 20260703034242 | calendar_event_cache | RENAME |
| 202607031200_key_result_progress | 20260703112522 | key_result_progress | RENAME |
| — (missing) | 20260703112851 | key_result_progress_user_index | ADOPT |
| 050_workout_logs (old numbered) | 20260708072708 | 050_workout_logs | version-drift (old numbered; see note) |
| — (missing) | 20260708073934 | 027_security_definer_lockdown_policy_fix | ADOPT |
| — (missing) | 20260708073942 | 050_workout_logs (**re-apply**) | ADOPT (double-apply) |
| 202607091200_debrief_reminder | 20260709043814 | debrief_reminder | RENAME |
| 202607091400_literature_custom_topics | — none — | — | **NOT IN PROD** — investigate/decide |
| 202607091600_supper_club_prefs | 20260709043834 | supper_club_prefs | RENAME |
| 202607131700_fund_provenance_columns | 20260713190519 | 202607131700_fund_provenance_columns | RENAME |
| 202607131701_tasks_table | 20260713190547 | 202607131701_**agent_tasks_table** | RENAME (name variant — confirm) |
| 202607131702_approvals_table | 20260713190604 | 202607131702_approvals_table | RENAME |
| 202607140100_routine_runs | 20260714022018 | 202607140100_routine_runs | RENAME |
| 202607140200_webauthn_challenges_approval_id | 20260714145829 | …webauthn_challenges_approval_id | RENAME |
| 202607140300_expire_stale_approvals | 20260714200800 | …expire_stale_approvals | RENAME |
| 202607150900_routine_run_pause_metadata | 20260715224141 | routine_run_pause_metadata | RENAME |
| 202607151000_fund_bank_transactions_provenance | — none — | — | **NOT IN PROD** — investigate/decide |
| 202607151100_routine_versions | 20260715224156 | routine_versions | RENAME (prod has `>0` check) |
| 202607151200_market_report_insights | 20260715235237 | market_report_insights | RENAME |
| 202607151300_make_delivery_outbox | 20260716002727 | make_delivery_outbox | RENAME |
| 202607151310_lock_expire_stale_approvals | 20260716002837 | lock_expire_stale_approvals | RENAME |
| 202607152100_mail_cache_sync | 20260716010134 | mail_cache_sync | RENAME |
| 202607152300_memory_center_profiles | — none — | — | forward-only KEEP (owner-confirmed) |
| 202607161200_entity_workspace | 20260716053709 | entity_workspace | RENAME |
| 202607161300_task_approval_atomic | 202607161300 | task_approval_atomic | EXACT (keep phase9 body) |
| 202607161302_webauthn_atomic | 202607161302 | webauthn_atomic | EXACT |
| 202607161400_routine_resume_claims | 202607161400 | routine_resume_claims | EXACT |
| 202607161401_task_approval_lockdown | — none — | — | deferred contract KEEP |

**Judgment calls needing owner input:**
1. `literature_custom_topics` (202607091400) & `fund_bank_transactions_provenance` (202607151000) — not in prod.
   Forward-only (apply later), or were they superseded/rolled back? Investigate before renaming anything else.
2. `tasks_table` vs prod's `agent_tasks_table` name — confirm the file rename target name.
3. `050_workout_logs` double-apply — adopt the `20260708073942` reapply as a no-op/idempotent file.
4. Pre-July numbered migrations (`001`–`050`, `202606*`) also carry version drift vs prod's 14-digit versions,
   but are long-applied and stable — lower priority; confirm whether in-scope.


Read-only. Nothing changed. Sources: `origin/codex/phase9-resume-executor@84cc5ca6`,
`origin/main@66b3eb0c`, live prod (Supabase project `twkcvyhmlguipchfetge`, `max(schema_migrations.version)=202607161400`).

## Headline finding
**Live prod is the only reliable canonical source — main's drift-backfill is itself imperfect.**
Proven: prod's `routine_versions` has `CHECK ((routine_version > 0))`. **phase9's file has it; main's
reconstructed file dropped it.** So "adopt main's backfilled version" is NOT safe as a blanket rule — every
reconstructed object must be validated against live prod (`pg_get_constraintdef` / `pg_get_functiondef` /
`information_schema` / `pg_indexes`), not against main's `.sql`.

The drift is primarily in **filenames** (phase9 used placeholder sequential timestamps; the real
apply-to-prod used different timestamps that main backfilled). phase9's **content** is, at least where
deep-checked, faithful to prod.

## Category A — Filename-drift pairs (same logical migration, two versions) — PROD-VERIFIED
**All 7: phase9 content == live prod. Reconciliation = pure RENAME (phase9's content → prod's canonical
version). main's reconstruction is the divergent side.** Delete phase9's placeholder-versioned duplicate.

| logical migration | phase9 file (placeholder) | prod canonical version | phase9-vs-prod verification |
|---|---|---|---|
| routine_run_pause_metadata | 202607150900 | 20260715224141 | ✅ prod has `idempotency_key`, `paused_step_key` cols + `idx_routine_runs_user_waiting_approval`, `idx_approvals_id_user`, `idx_routine_runs_user_idempotency_key` |
| routine_versions | 202607151100 | 20260715224156 | ✅ all 11 cols + `CHECK(routine_version>0)` + status check + `one_active` partial index (main dropped the check) |
| market_report_insights | 202607151200 | 20260715235237 | ✅ `ai_insights_kind_check` includes `market_report` (6 kinds, exact match) |
| make_delivery_outbox | 202607151300 | 20260716002727 | ✅ `integration_delivery_outbox` 15 cols + all CHECK/UNIQUE/FK + 2 indexes + 1 policy match |
| lock_expire_stale_approvals | 202607151310 | 20260716002837 | ✅ `expire_stale_approvals()` live (0-arg) |
| mail_cache_sync | 202607152100 | 20260716010134 | ✅ `integration_sync_state` (12 cols, 4 policies) + `mail_message_cache` (17 cols, 4 policies) + indexes match |
| entity_workspace | 202607161200 | 20260716053709 | ✅ `entity_references` (10 cols) + `entity_usage` (15 cols) + all checks/indexes/policies + 5 functions match |

## Category B — Same filename, body drift — PROD-VERIFIED
Same filename on both branches (no rename, no duplicate). **All 24 lifecycle functions are live in prod with
argument signatures matching phase9's authored definitions** (spot: `cas_agent_task_transition(p_user_id,
p_task_id, p_expected_status, p_next_status, p_completed_at)`, `create_idempotent_agent_task_with_activity(...,
p_idempotency_key)`, full passkey + routine-resume + guard family). The phase9↔main body-diff is purely
`pg_get_functiondef` normalization. **Canonical = phase9's authored files** (maintainable, match prod behavior);
resolve the merge conflict in phase9's favor.

| migration | functions verified live in prod |
|---|---|
| 202607161300_task_approval_atomic | cas_agent_task_transition, cas_approval_transition, create_agent_task_with_activity, create_idempotent_agent_task_with_activity, create_approval_with_activity, consume_actionable_approval, is_approval_scope_complete, expire_stale_approvals ✅ |
| 202607161302_webauthn_atomic | consume_webauthn_challenge, consume_approval_authentication_challenge, commit_passkey_authentication, commit_approval_step_up, create_user_passkey, delete_user_passkey ✅ |
| 202607161400_routine_resume_claims | claim/renew/release_routine_resume_claim, start/complete/fail_claimed_routine_step, complete/repause_routine_resume, guard_claimed_routine_run/step_browser_write ✅ |

Note: prod has the `guard_claimed_routine_*_browser_write` **functions** (created in 1400) but NOT the 1401
lockdown triggers/revokes (max applied = 202607161400). Consistent with 1401 remaining the deferred contract.

## Category C — phase9 is MISSING prod-applied migrations (must adopt from main)
| migration | prod status |
|---|---|
| 20260703112851_key_result_progress_user_index | index **confirmed live in prod** |
| 20260708073934_027_security_definer_lockdown_policy_fix | on main; verify in prod during reconcile |
| 20260708073942_050_workout_logs_reapply | on main; verify in prod during reconcile |

## Category D — phase9-only, NOT in prod (forward migrations, not drift)
| migration | prod status | disposition |
|---|---|---|
| 202607152300_memory_center_profiles | `memory_center*` tables **absent from prod** | genuinely new/unapplied — **owner question below** |
| 202607161401_task_approval_lockdown | contract; correctly not applied | keep (deferred contract) |

## Category E — VECTOR convergence additions (drift-free, validated vs prod)
- `202607170001_vector_arcade_persistence` — collision-free (`game_*` tables + arcade RPCs absent from prod). Keep.
- dropped `202607161000_lifecycle_claims` — correct (`create_agent_task` absent from prod).

## Canonical reconciliation rule (proposed)
For every Category A/B item: **reconstruct the object to match live prod exactly, file it under prod's
canonical `schema_migrations` version, and delete the phase9 placeholder-versioned duplicate.** No design
choice is involved for already-applied objects — prod is the truth. Category C: adopt the 3 prod migrations
into phase9. This is a **#195-level fix** (applies to the whole redesign branch, not just VECTOR); the VECTOR
convergence rebases onto the corrected phase9 afterward.

## Owner decisions (confirmed 2026-07-17)
1. **`memory_center_profiles` (202607152300)** — **KEEP** as an intended forward migration (phase9-new, not in prod).
2. **Contract `202607161401_task_approval_lockdown`** — **CONFIRMED deferred**; unchanged by this reconciliation.
3. **Intentional prod-schema changes** — **CONFIRMED**: any deliberate alteration of an already-applied object
   is a NEW forward migration with its own version, never a rewrite of a backfilled/reconciled file.

## Reconciliation execution spec (for the fix — NOT done in this read-only audit)
This is a **#195-level change** (whole redesign branch); the VECTOR convergence rebases onto the result.

**A. Rename the 7 Category-A files** (git mv, content unchanged — already prod-faithful):
```
202607150900_routine_run_pause_metadata.sql  -> 20260715224141_routine_run_pause_metadata.sql
202607151100_routine_versions.sql            -> 20260715224156_routine_versions.sql
202607151200_market_report_insights.sql      -> 20260715235237_market_report_insights.sql
202607151300_make_delivery_outbox.sql        -> 20260716002727_make_delivery_outbox.sql
202607151310_lock_expire_stale_approvals.sql -> 20260716002837_lock_expire_stale_approvals.sql
202607152100_mail_cache_sync.sql             -> 20260716010134_mail_cache_sync.sql
202607161200_entity_workspace.sql            -> 20260716053709_entity_workspace.sql
```
Update any references (release manifests, tests, docs) to the old versions.

**B. Category-B files (1300/1302/1400)** — no rename. On merge to main, resolve the conflict by keeping
phase9's authored bodies (verified to match prod behavior). Discard main's `pg_get_functiondef` reconstructions.

**C. Adopt the 3 prod migrations phase9 lacks** (take from main, verify present in prod during execution):
`20260703112851_key_result_progress_user_index`, `20260708073934_027_security_definer_lockdown_policy_fix`,
`20260708073942_050_workout_logs_reapply`.

**D. Keep phase9-only forward migrations:** `202607152300_memory_center_profiles`, `202607161401_task_approval_lockdown`.

**E. VECTOR:** `202607170001_vector_arcade_persistence` unchanged (still after max; collision-free).

**F. Fix main independently:** correct `20260715224156_routine_versions.sql` to restore prod's
`CHECK ((routine_version > 0))` (a fresh replay from main currently builds a divergent table).

**Post-fix invariant:** the union of all migration files must reproduce live prod exactly (through
`202607161400`), plus the deferred contract (`202607161401`) and forward-new (`memory_center_profiles`,
`202607170001`) that prod does not yet have. Verify with a fresh replay + schema diff once a stack is available.

## Renumbering caveat (ordering) — must verify on replay
phase9's placeholders (`2026071509xx`–`2026071523xx`) sort BEFORE the real prod versions (`20260715224141`+),
so the renames **do shift some relative orderings**. Concretely, the phase9-only `memory_center_profiles`
(`202607152300`, NOT renamed) currently sorts AFTER `mail_cache_sync` (`202607152100`); after `mail_cache_sync`
→ `20260716010134`, `memory_center_profiles` sorts BEFORE it. That specific inversion is harmless
(`memory_center_profiles` is independent of mail cache), but the general point stands: **a fresh migration
replay + schema diff against prod is mandatory** after renaming, to prove no dependency inversion and that the
union reproduces prod exactly. Also confirm `entity_workspace` (→ `20260716053709`) still precedes anything
that references entity objects (nothing in `1300`/`1302`/`1400` does).

## Correction needed on main regardless
`main`'s `20260715224156_routine_versions.sql` is missing prod's `CHECK ((routine_version > 0))`. Even
independent of #195, main's backfilled file should be corrected to match prod (a fresh replay from main would
build a `routine_versions` table that differs from production).
